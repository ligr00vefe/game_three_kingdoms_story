# 관우 자연어 명령 시스템

## 비용 원칙

- 게임의 필수 진행은 외부 유료 API에 의존하지 않는다.
- 이동, 전투, 경로, 대기 대사, 상태 보고는 모두 클라이언트 게임 로직에서 실행한다.
- 현재 구현에는 OpenAI 또는 다른 유료 API 호출 경로가 없다.
- 향후 자연어 해석 보강은 로컬 모델(Ollama 등)을 선택적으로 사용한다.
- 로컬 모델이 없거나 실패해도 기본 명령과 게임 진행은 정상 동작해야 한다.

## 처리 흐름

```text
채팅 입력
  → 무료 로컬 파서
  → GuanYuCommand 검증
  → GuanYuController 상태 전환
  → 기존 Player 이동·공격·애니메이션 실행
```

로컬 파서가 명확히 해석하지 못한 문장만 Spring Boot를 통해 localhost의 Ollama로 전달한다.
기본 모델은 `qwen3:4b`이며 외부 또는 유료 API fallback은 없다.

## 허용 행동

행동의 단일 코드 명세는 `frontend/src/game/ai/commands.ts`에 둔다.

- `CONTINUE_AUTO_COMBAT`: 가장 가까운 적을 찾아 계속 전투
- `MOVE_TO`: 지정된 안전한 위치 또는 앞·뒤 방향으로 일정 거리 이동한 뒤 정지
- `RETURN_TO_BASE`: 현재 맵의 본진 방향으로 복귀
- `GUARD_POSITION`: 지정 위치로 이동한 뒤 제한 반경 방어
- `ADVANCE_AND_ATTACK`: 전진하면서 발견한 적과 전투
- `ATTACK_TARGET`: 검증된 특정 적 공격
- `FOLLOW_PLAYER`: 향후 별도 지휘 캐릭터가 생길 때 사용
- `RETREAT`: 후방으로 이탈
- `HOLD`: 현재 위치에서 대기
- `USE_SKILL`: 검증된 보유 스킬 사용
- `JUMP`: 현재 진행 명령을 유지하면서 1회 점프
- `PLACE_BARRICADE`: 디펜스 아레나 대기 단계에서 캐릭터 앞에 방벽 설치
- `HOLD_AND_ATTACK`: 현재 위치에서 이동하지 않고 사거리 안의 적만 공격
- `PURSUE_ENEMIES`: 성에 가장 가까이 침투한 적을 우선 추격
- `ELIMINATE_CASTLE_INFILTRATORS`: 디펜스에서 성으로 침투하는 적을 위험 순서로 계속 섬멸
- `PRIORITIZE_CASTLE_DEFENSE`: 성문 기준 제한 반경만 출전하고 이탈 시 즉시 복귀
- `TALK_TO_NPC`: 검증된 NPC에게 이동해 기존 대화 실행
- `STATUS`: 로컬 게임 상태 보고
- `ANSWER_GAME_QUESTION`: 고정 게임 지식 답변
- `UNSUPPORTED`: 지원하지 않거나 불명확한 입력

## 초기 상태

- 관우는 감숙성 마을에서 `STANDBY`로 시작한다.
- 방향키·공격키의 직접 조작과 대화 명령을 함께 지원한다.
- 키보드 방향·점프·공격 입력이 들어오면 진행 중이던 대화 명령을 취소하고 직접 조작으로 전환한다.
- 기존 ↑키 NPC·포탈 상호작용을 유지한다.
- `STANDBY`와 `HOLDING` 중에는 승인된 대기 대사를 일정 간격으로 출력한다.
- 대기 대사는 외부 모델을 호출하지 않는다.

## 안전 규칙

1. 허용 목록 밖의 action은 `UNSUPPORTED`로 바꾼다.
2. 답변은 공백 제거 후 최대 80자로 제한한다.
3. 위치, NPC, 적 ID는 현재 게임 데이터에서 다시 확인한다.
4. 모델이 추가되더라도 모델 출력은 같은 검증기를 통과한다.
5. 명령을 확신할 수 없으면 임의 행동 대신 재입력을 요청한다.

## 단계별 확장 순서

1. 기본 상태 머신과 채팅 연결
2. 성문·성 밖·디펜스 맵 자동 전환
3. 특정 적 공격과 스킬 명령
4. 무료 로컬 의미 분류 또는 Ollama 연결
5. 복합 명령 평가 세트와 회귀 테스트
